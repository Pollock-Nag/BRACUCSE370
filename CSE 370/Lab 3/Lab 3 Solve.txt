1.select distinct(multiplier) from users;
2. select member_id,influence_count from users order by influence_count;
3. select distinct member_since from users order by member_since;
4.select name , email , member_since from users where member_since ="2020-07-1";
5.select name , email , member_since from users where member_since <"2020-07-1";
6.select name , email , member_since from users where member_since between "2020-07-1"and "2020-07-31";
7.select name , email , member_since from users where influence_count >=750000;
8.select name , email , member_since from users where member_since between "2020-07-31" and "2020-08-15" and influence_count>750000;
9.select member_id , email from users where email like '%com';
10.select member_id , email from users where email like '%djangoproject%';
11.select name , email from users where influence_count=(select max(influence_count)from users);
12.select name , email from users order by member_since desc limit 5;
13.select count(member_id) as "Total number of users" from users;
14.select count( distinct(multiplier)) as"Number of unique multipliers" from users;
15.select name from users where influence_count in (select max(influence_count)from users group by multiplier);
16.select name from users where influence_count in (select max(influence_count)from users group by multiplier);
17.select multiplier from users group by multiplier having count(*)>= all(select count(*) from users group by multiplier);
18.select multiplier from users group by multiplier having sum(influence_count)>= all(select sum(influence_count) from users group by multiplier);
19.select multiplier, count(*)as "Count Number" from users where month(member_since)>4 group by multiplier;
20.select name from users where influence_count>=700000 and MOD(multiplier,2)=0  order by name ;
